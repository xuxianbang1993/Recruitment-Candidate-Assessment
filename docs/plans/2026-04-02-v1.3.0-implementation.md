# v1.3.0 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 业务岗8维度重构 + AI评分差异化 + 报告附件上传与二次综合评估 + 3-5人对比

**Architecture:** 扩展 DimensionTemplate 增加 indicators 字段；强化 AI Prompt 注入分档标准和关键指标；新建 attachments 表 + DAO + API 实现附件管理和二次评估；升级 ResumeUploader 支持多文件批量上传。

**Tech Stack:** Svelte 5 + SvelteKit + TypeScript + SQLite (better-sqlite3) + Tailwind CSS v4

---

## Task 1: 扩展 DimensionTemplate 类型 + indicators 字段

**Files:**
- Modify: `src/lib/config/job-templates.ts:6` — DimensionTemplate 接口增加 indicators
- Modify: `src/lib/config/job-templates.ts:33-42` — management 补空 indicators
- Modify: `src/lib/config/job-templates.ts:59-71` — expert 补空 indicators
- Modify: `src/lib/config/job-templates.ts:73-86` — support 补空 indicators

**Step 1: 在 DimensionTemplate 接口中添加 indicators 字段**

```typescript
// src/lib/config/job-templates.ts:6-12
export interface DimensionTemplate {
  name: string
  weight: number
  definition: string
  positive: string
  negative: string
  questions: string[]
  indicators: string[]  // 新增
}
```

**Step 2: 为 management/expert/support 的每个维度补 `indicators: []`**

在 management、expert、support 三个模板的每个维度对象末尾加 `indicators: []`。

**Step 3: 运行类型检查确认**

Run: `cd ~/Documents/深圳SZOMK/奥科姆项目/Recruitment-Candidate-Assessment && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5`
Expected: 报错提示 sales 维度缺少 indicators（因为还没改 sales）

**Step 4: Commit**

```bash
git add src/lib/config/job-templates.ts
git commit -m "feat(types): add indicators field to DimensionTemplate"
```

---

## Task 2: 重写 Sales 模板 — 8 新维度 + KPI 指标

**Files:**
- Modify: `src/lib/config/job-templates.ts:44-57` — 替换 sales dimensions 数组

**Step 1: 替换 sales.dimensions 为以下 8 个维度**

```typescript
sales: {
  category: 'sales',
  label: '销售/业务岗',
  description: '普通业务、资深业务、大客户经理',
  dimensions: [
    {
      name: '目标感',
      weight: 15,
      definition: '以目标为驱动，关注结果，有强烈的达成意愿和分解能力',
      positive: '主动设定挑战目标，持续追踪进度，想方设法达成，为目标付出额外努力',
      negative: '对目标无感，完不成找借口，缺乏紧迫感，轻易放弃',
      questions: ['你上一份工作的业绩目标和达成情况？', '月中发现业绩落后你会怎么做？', '描述你为达成一个困难目标坚持到底的经历'],
      indicators: ['清晰的目标分解能力（年→季→月→周）', '为达成目标愿意付出的行为证据（加班、主动开发、调整策略）', '面对困难时的坚持力和毅力（不轻易放弃的实例）']
    },
    {
      name: '勤奋度',
      weight: 15,
      definition: '工作投入度高，主动付出额外时间和精力提升业绩',
      positive: '高出勤率，主动加班跟进客户，利用休息时间学习产品知识，行为量持续保持高位',
      negative: '准时下班从不加班，工作量刚好达标，不愿额外付出，缺乏紧迫感',
      questions: ['描述你最忙碌的一段工作经历，你是如何安排时间的？', '你的日常客户拜访量/电话量是多少？', '你是否有利用业余时间提升自己的习惯？'],
      indicators: ['加班/自主学习等额外时间投入频率', '主动出击频率（日均拜访量、电话量、邮件量）', '利用休息时间提升业务的行为（自学产品、研究市场、维护客户关系）']
    },
    {
      name: '沟通能力',
      weight: 15,
      definition: '清晰表达、有效倾听、推动协作达成共识',
      positive: '根据客户类型调整沟通方式，逻辑清晰，能快速建立信任，跨部门协作顺畅',
      negative: '表达混乱，只会单向输出不会倾听，跨部门沟通困难',
      questions: ['描述你通过沟通成功说服客户的经历', '客户提出不合理要求时你如何应对？', '你如何与工厂/技术部门协调交期问题？'],
      indicators: ['表达清晰度与逻辑性（能否简洁传递核心信息）', '跨部门/跨文化沟通效率（与工厂、物流、技术的协作）', '冲突调解与共识达成能力（客户投诉、内部分歧处理）']
    },
    {
      name: '抗压能力',
      weight: 10,
      definition: '在业绩压力和客户拒绝下保持积极状态和行动力',
      positive: '面对挫折迅速调整，保持高出勤和拜访量，情绪稳定不影响工作',
      negative: '遇到困难就消极，业绩不好时行为量下降，容易情绪化',
      questions: ['你经历过最大的业绩压力是什么？如何应对的？', '连续被客户拒绝后你的心理状态和行动？', '描述一次高压下你仍然完成任务的经历'],
      indicators: ['高压环境下的情绪稳定性（是否影响工作表现）', '连续遭拒后的恢复速度与行动力（多快重新出发）', '持续高强度工作的耐力（旺季/紧急项目期间的表现）']
    },
    {
      name: '学习能力',
      weight: 10,
      definition: '快速掌握新产品、新市场、新技能，并转化为业绩',
      positive: '主动学习行业新知，快速掌握新产品卖点，将学习成果应用到客户开发中',
      negative: '抗拒变化，重复犯同样错误，不愿接受新方法和新工具',
      questions: ['你最近学了什么与工作相关的新知识？如何应用？', '接手新产品线时你的学习方法是什么？', '举例你快速掌握新领域知识并产生业绩的经历'],
      indicators: ['新产品/新市场知识的掌握速度（从零到能独立介绍需要多久）', '学习方法论与自驱学习行为（是否有系统学习习惯）', '将新知识转化为业绩的能力（学以致用的实例）']
    },
    {
      name: '销售能力（业务、英语水平）',
      weight: 20,
      definition: '综合业务开发能力，包括英语沟通、谈判成交和客户开发',
      positive: '英语沟通流利，谈判有策略有底线，持续开发新客户，转化率高，订单额持续增长',
      negative: '英语无法应对商务场景，谈判中轻易让步，等客户上门不主动开发',
      questions: ['请用英语简单介绍你的工作经历（现场测试）', '描述你成功开发一个大客户的全过程', '你以往的年度/季度订单总额大概是多少？', '描述一次艰难谈判你是如何成交的'],
      indicators: ['英语口语沟通水平（日常/商务/谈判级别，是否能独立接待外商）', '谈判能力与成交技巧（报价策略、异议处理、促成签约）', '客户开发能力（新客户获取渠道和方法、开发成功率）', '以往订单总额/业绩数据（可量化的销售成果）']
    },
    {
      name: '价值观',
      weight: 10,
      definition: '认同公司核心价值观，诚信正直，遵守职业道德',
      positive: '言行一致，主动维护团队利益，面对利益冲突选择正确做法，客户关系健康透明',
      negative: '推诿责任，背后议论同事，为个人利益损害团队，客户关系不透明',
      questions: ['你在工作中遇到利益冲突时如何处理？', '你如何看待带走客户资源跳槽的行为？', '描述一次你主动承认错误的经历'],
      indicators: ['诚信正直的行为表现（是否有隐瞒、欺骗、飞单等行为）', '团队协作精神与利他行为（是否愿意分享经验、帮助同事）', '对公司文化和制度的认同度（是否遵守流程、维护公司形象）']
    },
    {
      name: '管理潜力',
      weight: 5,
      definition: '展现出领导力萌芽，有带团队和规划组织的潜质',
      positive: '主动带新人，能组织小型活动/项目，同事愿意跟随，有全局观',
      negative: '只关注个人业绩，不愿分享或帮助他人，缺乏全局视角',
      questions: ['你有带新人或带小团队的经历吗？', '你觉得一个好的销售主管应该具备什么能力？', '如果让你管理3-5人的小团队，你会怎么安排工作？'],
      indicators: ['领导力萌芽表现（是否主动带新人、组织团队活动）', '团队影响力与号召力（同事是否愿意听从建议）', '规划组织能力的初步体现（是否展现过项目管理或资源协调能力）']
    },
  ],
},
```

**Step 2: 运行类型检查**

Run: `npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5`
Expected: 0 errors

**Step 3: Commit**

```bash
git add src/lib/config/job-templates.ts
git commit -m "feat(config): rewrite sales template with 8 dimensions and KPI indicators"
```

---

## Task 3: 强化 AI 评估 Prompt — 差异化评分

**Files:**
- Modify: `src/lib/server/services/ai/prompts.ts:9-29` — buildBehaviorContext 增加 indicators
- Modify: `src/lib/server/services/ai/prompts.ts:32-78` — buildEvaluationPrompt 增加分档标准+差异化指令
- Modify: `src/lib/server/services/ai/prompts.ts:81-124` — buildReportPrompt 同步 indicators

**Step 1: 修改 buildBehaviorContext — 输出 indicators**

在 `mode === 'evaluation'` 分支中，将每个维度的 indicators 也输出：

```typescript
if (mode === 'evaluation') {
  return [
    '## 评分参考（行为锚点）',
    ...template.dimensions.map(
      (d) => {
        const base = `- ${d.name}：正向「${d.positive}」；反向「${d.negative}」`
        if (d.indicators && d.indicators.length > 0) {
          return base + '\n  关键指标：' + d.indicators.join('；')
        }
        return base
      }
    ),
  ].join('\n')
}
```

report 模式同理增加 indicators 输出。

**Step 2: 修改 buildEvaluationPrompt — 维度列表包含 indicators**

将维度列表格式从：
```
- 目标感（权重 15%）
```
改为：
```
- 目标感（权重 15%）
  关键指标：清晰的目标分解能力；为达成目标愿意付出的行为证据；面对困难时的坚持力
```

```typescript
const dimensionsList = [
  job.weights
    .map((dim) => {
      const tmpl = job.category ? getDimensionTemplate(job.category as JobCategory, dim.name) : undefined
      const base = `- ${dim.name}（权重 ${normalizeWeight(dim.weight)}%）`
      if (tmpl?.indicators && tmpl.indicators.length > 0) {
        return base + '\n  关键指标：' + tmpl.indicators.join('；')
      }
      return base
    })
    .join('\n'),
  buildBehaviorContext(job, 'evaluation'),
].filter(Boolean).join('\n\n')
```

需要在文件顶部导入 `getDimensionTemplate` 和 `JobCategory`。

**Step 3: 在 buildEvaluationPrompt 末尾注入分档标准和差异化指令**

在 `return` 的模板字符串中，`## 返回格式` 之前插入：

```typescript
## 分档标准（必须严格执行）
- 90-100：该维度表现卓越，简历中有多条明确的优秀行为证据
- 70-89：该维度表现良好，有 2-3 条正面行为证据支撑
- 50-69：该维度表现一般，证据不足或正反参半
- 30-49：该维度表现较差，多数指标不达标或缺乏证据
- 0-29：该维度严重不足，几乎无正面证据

## 评分规则（必须遵守）
1. 简历中无法找到某维度的支撑证据，该维度得分不得超过 40
2. 每个维度必须基于关键指标逐条评判，不可笼统给分
3. 优秀候选人与不合格候选人的总分差距应在 20 分以上
4. 禁止所有维度给出相近分数，必须体现维度间的强弱差异
5. 对于"销售能力"维度，如简历未体现英语水平或订单数据，该项不得超过 35
```

**Step 4: 运行类型检查**

Run: `npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5`
Expected: 0 errors

**Step 5: Commit**

```bash
git add src/lib/server/services/ai/prompts.ts
git commit -m "feat(ai): enhance evaluation prompt with scoring tiers and differentiation rules"
```

---

## Task 4: 数据库 Migration — attachments 表 + assessment 扩展

**Files:**
- Create: `src/lib/server/db/migrations/003-add-attachments-and-assessment-type.sql`

**Step 1: 创建 migration 文件**

```sql
-- 003: Add attachments table and assessment type/parent_id columns

ALTER TABLE assessments ADD COLUMN type TEXT NOT NULL DEFAULT 'initial';
ALTER TABLE assessments ADD COLUMN parent_id TEXT REFERENCES assessments(id);

CREATE TABLE IF NOT EXISTS attachments (
  id TEXT PRIMARY KEY,
  assessment_id TEXT NOT NULL REFERENCES assessments(id) ON DELETE CASCADE,
  filename TEXT NOT NULL,
  original_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_type TEXT NOT NULL,
  file_size INTEGER NOT NULL,
  text_content TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE INDEX IF NOT EXISTS idx_attachments_assessment ON attachments(assessment_id);
CREATE INDEX IF NOT EXISTS idx_assessments_parent ON assessments(parent_id);
```

**Step 2: Commit**

```bash
git add src/lib/server/db/migrations/003-add-attachments-and-assessment-type.sql
git commit -m "feat(db): add migration 003 — attachments table and assessment type"
```

---

## Task 5: Assessment 类型扩展 + DAO 更新

**Files:**
- Modify: `src/lib/types/assessment.ts:1-12` — Assessment 增加 type/parentId
- Modify: `src/lib/server/db/assessment-dao.ts` — rowToAssessment 映射新字段, create 支持新字段

**Step 1: 扩展 Assessment 类型**

```typescript
export interface Assessment {
  id: string
  candidateId: string
  jobId: string
  scores: ScoreDimension[]
  totalScore: number
  strengths: string[]
  weaknesses: string[]
  suggestions: string[]
  aiProvider: string
  type: 'initial' | 'comprehensive'  // 新增
  parentId: string | null             // 新增
  createdAt: string
}
```

**Step 2: 更新 assessment-dao.ts**

- `AssessmentRow` 增加 `type` 和 `parent_id`
- `rowToAssessment` 映射 `type: row.type ?? 'initial'`, `parentId: row.parent_id ?? null`
- `create` 的 INSERT 和 `.run()` 增加 type 和 parent_id 参数
- `create` 参数中 type 默认为 `'initial'`

**Step 3: 运行类型检查，修复引用 Assessment 但未提供 type/parentId 的地方**

Run: `npx svelte-check --tsconfig ./tsconfig.json 2>&1 | head -30`

主要需要修复 `evaluation-parser.ts` 中 fallback 返回值增加 type 和 parentId 默认值。

**Step 4: Commit**

```bash
git add src/lib/types/assessment.ts src/lib/server/db/assessment-dao.ts src/lib/server/services/ai/evaluation-parser.ts
git commit -m "feat(types): extend Assessment with type and parentId fields"
```

---

## Task 6: 创建 AttachmentDAO

**Files:**
- Create: `src/lib/server/db/attachment-dao.ts`
- Modify: `src/lib/server/db/index.ts` — 导出 attachmentDAO

**Step 1: 创建 attachment-dao.ts**

```typescript
import { getDatabase } from './database.js'
import { randomUUID } from 'crypto'

export interface Attachment {
  id: string
  assessmentId: string
  filename: string
  originalName: string
  filePath: string
  fileType: string
  fileSize: number
  textContent: string | null
  createdAt: string
}

interface AttachmentRow {
  id: string
  assessment_id: string
  filename: string
  original_name: string
  file_path: string
  file_type: string
  file_size: number
  text_content: string | null
  created_at: string
}

function rowToAttachment(row: AttachmentRow): Attachment {
  return {
    id: row.id,
    assessmentId: row.assessment_id,
    filename: row.filename,
    originalName: row.original_name,
    filePath: row.file_path,
    fileType: row.file_type,
    fileSize: row.file_size,
    textContent: row.text_content,
    createdAt: row.created_at,
  }
}

export class AttachmentDAO {
  getByAssessmentId(assessmentId: string): Attachment[] {
    const db = getDatabase()
    const rows = db
      .prepare('SELECT * FROM attachments WHERE assessment_id = ? ORDER BY created_at ASC')
      .all(assessmentId) as AttachmentRow[]
    return rows.map(rowToAttachment)
  }

  create(data: Omit<Attachment, 'id' | 'createdAt'>): Attachment {
    const db = getDatabase()
    const id = randomUUID()
    db.prepare(
      `INSERT INTO attachments (id, assessment_id, filename, original_name, file_path, file_type, file_size, text_content)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?)`
    ).run(id, data.assessmentId, data.filename, data.originalName, data.filePath, data.fileType, data.fileSize, data.textContent)
    const row = db.prepare('SELECT * FROM attachments WHERE id = ?').get(id) as AttachmentRow
    return rowToAttachment(row)
  }

  delete(id: string): void {
    const db = getDatabase()
    db.prepare('DELETE FROM attachments WHERE id = ?').run(id)
  }
}

export const attachmentDAO = new AttachmentDAO()
```

**Step 2: 更新 db/index.ts 导出**

添加: `export { AttachmentDAO, attachmentDAO } from './attachment-dao.js'`
添加: `export type { Attachment } from './attachment-dao.js'`

**Step 3: Commit**

```bash
git add src/lib/server/db/attachment-dao.ts src/lib/server/db/index.ts
git commit -m "feat(db): add AttachmentDAO for attachment CRUD"
```

---

## Task 7: 附件上传 API 路由

**Files:**
- Create: `src/routes/api/assessments/[id]/attachments/+server.ts`

**Step 1: 创建 API 路由 — GET（列表）+ POST（上传）+ DELETE**

```typescript
import { json } from '@sveltejs/kit'
import type { RequestHandler } from './$types'
import { assessmentDAO, attachmentDAO } from '$lib/server/db'
import { parseResume } from '$lib/server/services/resume'
import { randomUUID } from 'crypto'
import { writeFileSync, mkdirSync, unlinkSync } from 'fs'
import { join } from 'path'

const MAX_FILE_SIZE = 10 * 1024 * 1024
const ATTACHMENTS_DIR = join(process.cwd(), 'data', 'attachments')

function ensureDir(dir: string) {
  mkdirSync(dir, { recursive: true })
}

export const GET: RequestHandler = ({ params }) => {
  try {
    const assessment = assessmentDAO.getById(params.id)
    if (!assessment) {
      return json({ success: false, error: 'Assessment not found' }, { status: 404 })
    }
    const attachments = attachmentDAO.getByAssessmentId(params.id)
    return json({ success: true, data: attachments })
  } catch (e) {
    console.error('GET attachments error:', e)
    return json({ success: false, error: '服务器内部错误' }, { status: 500 })
  }
}

export const POST: RequestHandler = async ({ params, request }) => {
  const assessment = assessmentDAO.getById(params.id)
  if (!assessment) {
    return json({ success: false, error: 'Assessment not found' }, { status: 404 })
  }

  let formData: FormData
  try {
    formData = await request.formData()
  } catch {
    return json({ success: false, error: 'Invalid form data' }, { status: 400 })
  }

  const file = formData.get('file')
  if (!(file instanceof File)) {
    return json({ success: false, error: '请上传文件' }, { status: 400 })
  }
  if (file.size > MAX_FILE_SIZE) {
    return json({ success: false, error: '文件大小超出限制（最大 10MB）' }, { status: 413 })
  }

  try {
    const buffer = Buffer.from(await file.arrayBuffer())
    const ext = file.name.split('.').pop()?.toLowerCase() ?? ''
    const storedName = `${randomUUID()}.${ext}`

    ensureDir(ATTACHMENTS_DIR)
    const filePath = join(ATTACHMENTS_DIR, storedName)
    writeFileSync(filePath, buffer)

    // Determine file type category
    const docExts = ['pdf', 'docx', 'doc', 'txt']
    const audioExts = ['mp3', 'wav', 'm4a']
    const imageExts = ['jpg', 'jpeg', 'png']
    let fileType = 'other'
    if (docExts.includes(ext)) fileType = ext === 'txt' ? 'txt' : ext === 'pdf' ? 'pdf' : 'docx'
    else if (audioExts.includes(ext)) fileType = 'audio'
    else if (imageExts.includes(ext)) fileType = 'image'

    // Extract text for document types
    let textContent: string | null = null
    if (docExts.includes(ext)) {
      try {
        const parsed = await parseResume(buffer, file.name)
        textContent = parsed.text
      } catch {
        // Non-fatal: store without text
      }
    }

    const attachment = attachmentDAO.create({
      assessmentId: params.id,
      filename: storedName,
      originalName: file.name,
      filePath,
      fileType,
      fileSize: file.size,
      textContent,
    })

    return json({ success: true, data: attachment }, { status: 201 })
  } catch (e) {
    console.error('POST attachment error:', e)
    return json({ success: false, error: '上传失败' }, { status: 500 })
  }
}

export const DELETE: RequestHandler = ({ params, url }) => {
  const attachmentId = url.searchParams.get('attachmentId')
  if (!attachmentId) {
    return json({ success: false, error: 'Missing attachmentId' }, { status: 400 })
  }
  try {
    attachmentDAO.delete(attachmentId)
    return json({ success: true })
  } catch (e) {
    console.error('DELETE attachment error:', e)
    return json({ success: false, error: '删除失败' }, { status: 500 })
  }
}
```

**Step 2: 在 .gitignore 添加 `data/` 目录**

```
# Attachment storage
data/
```

**Step 3: Commit**

```bash
git add src/routes/api/assessments/\[id\]/attachments/+server.ts .gitignore
git commit -m "feat(api): add attachment upload/list/delete endpoints"
```

---

## Task 8: 二次综合评估 Prompt + API

**Files:**
- Modify: `src/lib/server/services/ai/prompts.ts` — 新增 buildReEvaluationPrompt
- Create: `src/routes/api/assessments/[id]/re-evaluate/+server.ts`

**Step 1: 在 prompts.ts 末尾新增 buildReEvaluationPrompt**

```typescript
export function buildReEvaluationPrompt(
  candidate: Candidate,
  job: Job,
  initialAssessment: Assessment,
  attachmentTexts: string[]
): string {
  const dimList = job.weights
    .map((dim) => {
      const tmpl = job.category ? getDimensionTemplate(job.category as JobCategory, dim.name) : undefined
      const base = `- ${dim.name}（权重 ${normalizeWeight(dim.weight)}%）`
      if (tmpl?.indicators && tmpl.indicators.length > 0) {
        return base + '\n  关键指标：' + tmpl.indicators.join('；')
      }
      return base
    })
    .join('\n')

  const initialScores = initialAssessment.scores
    .map(s => `- ${s.name}：${s.score} 分（权重 ${normalizeWeight(s.weight)}%）`)
    .join('\n')

  const attachmentContent = attachmentTexts.length > 0
    ? attachmentTexts.map((t, i) => `### 补充材料 ${i + 1}\n${t}`).join('\n\n')
    : '无补充材料'

  return `你现在拥有该候选人的简历和面试补充材料。请综合所有信息重新评分。

## 候选人信息
- 姓名：${candidate.name}
- 应聘岗位：${candidate.position}
- 简历内容：
${candidate.resumeText}

## 初评结果（仅供参考，需根据补充材料调整）
- 初评总分：${initialAssessment.totalScore}
${initialScores}

## 面试补充材料
${attachmentContent}

## 评分维度（满分 100）
${dimList}

${buildBehaviorContext(job, 'evaluation')}

## 分档标准（必须严格执行）
- 90-100：该维度表现卓越，简历+面试中有多条明确的优秀行为证据
- 70-89：该维度表现良好，有 2-3 条正面行为证据支撑
- 50-69：该维度表现一般，证据不足或正反参半
- 30-49：该维度表现较差，多数指标不达标或缺乏证据
- 0-29：该维度严重不足，几乎无正面证据

## 综合评估规则（必须遵守）
1. 面试表现证据的权重应高于简历自述
2. 如面试中发现简历内容不实或表现与简历不符，必须大幅调整分数
3. 简历中无法找到某维度的支撑证据，且面试材料也无相关表现，该维度得分不得超过 35
4. 每个维度必须基于关键指标逐条评判
5. 禁止所有维度给出相近分数，必须体现维度间的强弱差异

## 返回格式（严格 JSON，不要使用 Markdown 代码块）
{
  "scores": [
    { "name": "维度名称", "weight": 权重数字, "score": 0-100 }
  ],
  "totalScore": 加权总分,
  "strengths": ["优势1", "优势2"],
  "weaknesses": ["不足1", "不足2"],
  "suggestions": ["建议1", "建议2"]
}`
}
```

**Step 2: 创建 re-evaluate API 路由**

```typescript
// src/routes/api/assessments/[id]/re-evaluate/+server.ts
import { json } from '@sveltejs/kit'
import type { RequestHandler } from './$types'
import { assessmentDAO, candidateDAO, jobDAO, attachmentDAO } from '$lib/server/db'
import { createAI, AIServiceError } from '$lib/server/services/ai'
import { getAIConfig, AIConfigError } from '../../../ai/utils'
import { buildReEvaluationPrompt } from '$lib/server/services/ai/prompts'
import { safeParseEvaluation } from '$lib/server/services/ai/evaluation-parser'

export const POST: RequestHandler = async ({ params }) => {
  try {
    const assessment = assessmentDAO.getById(params.id)
    if (!assessment) {
      return json({ success: false, error: 'Assessment not found' }, { status: 404 })
    }

    const candidate = candidateDAO.getById(assessment.candidateId)
    if (!candidate) {
      return json({ success: false, error: 'Candidate not found' }, { status: 404 })
    }

    const job = jobDAO.getById(assessment.jobId)
    if (!job) {
      return json({ success: false, error: 'Job not found' }, { status: 404 })
    }

    // Gather attachment texts
    const attachments = attachmentDAO.getByAssessmentId(params.id)
    const attachmentTexts = attachments
      .map(a => a.textContent)
      .filter((t): t is string => t !== null && t.length > 0)

    if (attachmentTexts.length === 0) {
      return json({ success: false, error: '无可分析的附件文本内容' }, { status: 400 })
    }

    const config = getAIConfig()
    const ai = createAI(config)
    const prompt = buildReEvaluationPrompt(candidate, job, assessment, attachmentTexts)
    const rawResponse = await ai.chat([{ role: 'user', content: prompt }])
    const parsed = safeParseEvaluation(rawResponse, job)

    const comprehensive = assessmentDAO.create({
      candidateId: assessment.candidateId,
      jobId: assessment.jobId,
      scores: parsed.scores,
      totalScore: parsed.totalScore,
      strengths: parsed.strengths,
      weaknesses: parsed.weaknesses,
      suggestions: parsed.suggestions,
      aiProvider: config.provider,
      type: 'comprehensive',
      parentId: assessment.id,
    })

    return json({ success: true, data: comprehensive }, { status: 201 })
  } catch (e) {
    if (e instanceof AIConfigError) {
      return json({ success: false, error: e.message }, { status: 422 })
    }
    if (e instanceof AIServiceError) {
      return json({ success: false, error: e.message }, { status: 502 })
    }
    console.error('POST re-evaluate error:', e)
    return json({ success: false, error: '综合评估失败' }, { status: 500 })
  }
}
```

**Step 3: Commit**

```bash
git add src/lib/server/services/ai/prompts.ts src/routes/api/assessments/\[id\]/re-evaluate/+server.ts
git commit -m "feat(ai): add re-evaluation prompt and API endpoint"
```

---

## Task 9: 附件上传 UI 组件 — AttachmentUploader + AttachmentList

**Files:**
- Create: `src/lib/components/AttachmentUploader.svelte`
- Create: `src/lib/components/AttachmentList.svelte`

**Step 1: 创建 AttachmentUploader.svelte**

支持多文件拖拽+点击上传，接受 pdf/docx/txt/mp3/wav/m4a/jpg/png，调用 `/api/assessments/[id]/attachments` POST。

Props: `assessmentId: string`, `onupload: (attachment) => void`

**Step 2: 创建 AttachmentList.svelte**

列表展示已上传附件（文件名、类型图标、大小、删除按钮），调用 DELETE 端点。

Props: `attachments: Attachment[]`, `ondelete: (id: string) => void`

**Step 3: Commit**

```bash
git add src/lib/components/AttachmentUploader.svelte src/lib/components/AttachmentList.svelte
git commit -m "feat(ui): add AttachmentUploader and AttachmentList components"
```

---

## Task 10: 报告页集成 — 附件上传 + 二次评估

**Files:**
- Modify: `src/routes/reports/+page.svelte`

**Step 1: 在报告页中集成附件功能**

在生成报告区域下方增加：
1. 附件上传区域（`AttachmentUploader`）
2. 已上传附件列表（`AttachmentList`）
3. "综合评估" 按钮 — 调用 `/api/assessments/[id]/re-evaluate`
4. 综合评估结果展示（替代/并列显示）
5. Loading 状态指示

**Step 2: 添加状态管理**

```typescript
import type { Attachment } from '$lib/server/db/attachment-dao'
let attachments = $state<Attachment[]>([])
let reEvaluating = $state(false)
let comprehensiveAssessment = $state<Assessment | null>(null)
```

**Step 3: 选择评估记录后自动加载附件列表**

**Step 4: 附件上传后自动触发二次评估**

**Step 5: Commit**

```bash
git add src/routes/reports/+page.svelte
git commit -m "feat(ui): integrate attachments and re-evaluation in reports page"
```

---

## Task 11: ResumeUploader 支持多文件批量上传

**Files:**
- Modify: `src/lib/components/ResumeUploader.svelte`

**Step 1: 添加 `multiple` prop**

```typescript
let { onupload, multiple = false }: {
  onupload: (candidate: Candidate) => void
  multiple?: boolean
} = $props()
```

**Step 2: file input 增加 multiple 属性**

```html
<input type="file" accept=".pdf,.doc,.docx,.txt" class="hidden" {multiple} ... />
```

**Step 3: handleDrop 和 handleFileChange 处理多文件**

遍历 `files` 数组，串行调用 `uploadFile()`，每次成功调用 `onupload`。

**Step 4: 增加上传进度显示**

显示 "正在上传 2/5..." 进度文字。

**Step 5: Commit**

```bash
git add src/lib/components/ResumeUploader.svelte
git commit -m "feat(ui): support multi-file upload in ResumeUploader"
```

---

## Task 12: 评估结果页 — 多选候选人对比

**Files:**
- Modify: `src/routes/assessment/results/+page.svelte`
- Modify: `src/lib/components/CandidateCompare.svelte`

**Step 1: 在 results 页面 overview 标签中，每个 ScoreCard 前增加复选框**

```typescript
let selectedForCompare = $state<Set<string>>(new Set())
```

勾选后底部浮现 "对比 N 人" 按钮（限 2-5 人），点击切换到 compare 标签并传入选中的 assessments。

**Step 2: CandidateCompare 优化横向滚动**

增加 `max-width` 和 `overflow-x-auto`，超过 3 人时横滑。

**Step 3: Commit**

```bash
git add src/routes/assessment/results/+page.svelte src/lib/components/CandidateCompare.svelte
git commit -m "feat(ui): add multi-select candidate comparison (3-5 people)"
```

---

## Task 13: 类型检查 + 全局验证

**Step 1: 运行 svelte-check**

Run: `npx svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors

**Step 2: 删除旧数据库触发 migration 重建**

Run: `rm -f recruitment.db recruitment.db-wal recruitment.db-shm`

**Step 3: 启动开发服务器验证**

Run: `npm run dev`
Expected: 无报错启动

**Step 4: 最终 Commit + Tag**

```bash
git add -A
git commit -m "chore: v1.3.0 final cleanup and verification"
git tag v1.3.0
```
