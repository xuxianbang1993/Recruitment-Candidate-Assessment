# v1.3.0 审核问题修复计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修复 Opus 三方审核发现的所有 CRITICAL/MAJOR/MINOR 问题

**Architecture:** 修复模板嵌套错误、XSS 漏洞、分层违规、设计令牌硬编码、Svelte 5 废弃 API、代码质量问题

**Tech Stack:** Svelte 5, SvelteKit, TypeScript, DOMPurify, Tailwind CSS v4

---

## Task 1: 安装 DOMPurify (C2 前置)

Run: `npm install dompurify && npm install -D @types/dompurify`

Commit: `fix(deps): add DOMPurify for XSS sanitization`

---

## Task 2: 创建公共 Attachment 类型 (M1)

Create: `src/lib/types/attachment.ts`

将 Attachment 接口从 attachment-dao.ts 移至公共类型目录，DAO 和组件都从这里导入。

Commit: `fix(types): move Attachment interface to public types directory`

---

## Task 3: 修复 reports/+page.svelte (C1 + C2 + M2 + M3)

**最关键的修复**：
1. 将附件上传+二次评估区块从 `{:else if candidates.length === 0}` 内部移出，作为独立区块
2. 使用 DOMPurify 消毒 {@html reportText}
3. 替换所有硬编码颜色为设计令牌
4. Attachment 类型从 `$lib/types` 导入

Commit: `fix(ui): restructure reports page — attachment visibility, XSS, design tokens`

---

## Task 4: 修复 ReportProse.svelte (C2)

使用 DOMPurify 消毒 {@html html}。

Commit: `fix(security): sanitize HTML in ReportProse with DOMPurify`

---

## Task 5: 修复 Sidebar.svelte + layout.svelte (M4)

将 `import { page } from '$app/stores'` 迁移为 `import { page } from '$app/state'`，移除 `$` 前缀。

Commit: `fix(ui): migrate $app/stores to $app/state for Svelte 5`

---

## Task 6: 修复 AttachmentUploader.svelte (M1 + M2)

1. import Attachment from `$lib/types`
2. 替换硬编码颜色

Commit: `fix(ui): fix layer violation and design tokens in AttachmentUploader`

---

## Task 7: 修复 AttachmentList.svelte (M1 + M2)

1. import Attachment from `$lib/types`
2. 替换硬编码颜色

Commit: `fix(ui): fix layer violation and design tokens in AttachmentList`

---

## Task 8: 修复 candidates/+page.svelte (M5 + M2)

1. handleUpload 参数类型改为 Candidate
2. 替换硬编码颜色

Commit: `fix(ui): fix type safety and design tokens in candidates page`

---

## Task 9: 修复 ResumeUploader.svelte (M2)

替换硬编码颜色为设计令牌。

Commit: `fix(ui): replace hardcoded colors with design tokens in ResumeUploader`

---

## Task 10: 修复 assessment/results/+page.svelte (M2 + M3)

替换硬编码颜色为设计令牌（文件较长但因 Tab 结构不宜拆分）。

Commit: `fix(ui): replace hardcoded colors with design tokens in results page`

---

## Task 11: 修复 CandidateCompare.svelte (M2)

scoreColor 函数使用 CSS 变量。

Commit: `fix(ui): replace hardcoded colors with design tokens in CandidateCompare`

---

## Task 12: 修复 +layout.svelte (M2)

替换硬编码颜色。

Commit: `fix(ui): replace hardcoded colors with design tokens in layout`

---

## Task 13: 修复 app.css body 样式 (M2)

body 中使用 CSS 变量。

Commit: `fix(ui): use design tokens in app.css body styles`

---

## Task 14: 修复 database.ts migration 事务 (M6)

将 migration 执行和记录包裹在事务中。

Commit: `fix(db): wrap migrations in transactions for atomic execution`

---

## Task 15: 附件扩展名白名单校验 (m1)

在 attachments API 中添加扩展名白名单。

Commit: `fix(api): add file extension whitelist for attachment uploads`

---

## Task 16: 类型检查验证

Run: `npx svelte-check --tsconfig ./tsconfig.json`
Expected: 0 errors
